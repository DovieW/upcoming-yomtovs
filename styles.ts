// styles.ts
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  linkText: {
    color: 'teal',
  },
  Card: {
    margin: 10,
  },
  List: {
    flex: 1,
  },
});

export default styles;
